spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartchess?useSSL=false&serverTimezone=Asia/Taipei&characterEncoding=utf8
    username: root
    password: Shadower1120

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
